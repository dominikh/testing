on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  foo:
    name: some job
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - run: "ls -la /c/Users/runneradmin/AppData/Local/go-build"
        shell: "bash"
      - name: Run Staticcheck
        uses: dominikh/staticcheck-action@wip
